import { QueryCommandBuilder } from "src/query/builder/query.builder";
import { eUser } from "./test.entity";


// it("", () => {
//   const builder = QueryCommandBuilder.from(eUser, {
    
//   });

// console.log(JSON.stringify(s4, null, 2));

// const builder = new QueryCommandBuilder(s4);

// const bb = builder.toQuery();

// console.log(JSON.stringify(bb, null, 2));

// })